---
description: Navigate the Takumi kit — find the right skill and workflow for your task
argument-hint: "[task description | skill-name | --list | --workflow]"
---

Activate the `help` skill.

<task>$ARGUMENTS</task>

Route the task above to the best Takumi skill and workflow. If no task is provided, ask the user what they are trying to accomplish.

Modes:
- `--list` — show all skills organized by domain
- `--workflow` — show all standard workflow chains
- `<skill-name>` (e.g. `brainstorm`, `fix-bug`) — explain that skill in depth
- `<task description>` — recommend the right skill + workflow + example invocation
